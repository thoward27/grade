name: Test with Docker

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        docker build --rm -t grade:latest .
        docker run --rm -it grade:latest
